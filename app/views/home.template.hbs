<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Welcome to {{title}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', 'Roboto', Arial, sans-serif;
            background:
                linear-gradient(135deg, #0f1f0f, #0b2b16 60%, #0a3d2a);
            color: #eaf7ee;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            text-rendering: optimizeLegibility;
            padding: 24px;
        }

        .container {
            text-align: center;
            padding: 2rem;
            background-color: rgba(0, 0, 0, 0.6);
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: #ffe57f;
        }

        p {
            font-size: 1.2rem;
            margin: 1rem 0 1.5rem;
            color: #e0f7fa;
        }

        .button {
            display: inline-block;
            padding: 0.9rem 2rem;
            font-size: 1rem;
            font-weight: 700;
            color: #0b2b16;
            background: linear-gradient(135deg, #c8ff9e, #7cffc4);
            border: none;
            border-radius: 10px;
            cursor: pointer;
            transition: transform 0.2s ease, box-shadow 0.2s ease, filter 0.2s ease;
            text-decoration: none;
            box-shadow: 0 10px 24px rgba(124, 255, 196, 0.2);
        }

        .button:hover {
            transform: translateY(-2px);
            filter: brightness(1.03);
            box-shadow: 0 14px 28px rgba(124, 255, 196, 0.25);
        }

        .button--secondary {
            color: #b2ff59;
            background: transparent;
            border: 1px solid rgba(178, 255, 89, 0.45);
            box-shadow: none;
        }

        .button--secondary:hover {
            background: rgba(178, 255, 89, 0.08);
            transform: translateY(-2px);
        }

        .actions {
            display: flex;
            gap: 12px;
            align-items: center;
            justify-content: center;
            flex-wrap: wrap;
        }

        @keyframes floatUp {
            from {
                transform: translateY(8px);
                opacity: 0;
            }

            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2.5rem;
            }

            p {
                font-size: 1rem;
            }

            .button {
                padding: 0.6rem 1.5rem;
                font-size: 0.9rem;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>{{title}}!</h1>
        <p>Application is running successfully. Everything is set up <br> and ready to go!</p>

        <div class="actions">
            <a href="/healthz" class="button button--secondary">Health Check</a>
            <a href="{{explore_url}}" class="button">Explore</a>
        </div>
    </div>
</body>

</html>